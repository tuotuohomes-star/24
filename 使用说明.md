# 上市产品价格合并工具

这是一个支持 macOS 和 Windows 的本地工具。两份源 Excel 的读取、匹配、预览和导出全部在浏览器内完成，数据不会上传到服务器。

## 使用方法

1. 双击 `上市产品价格合并工具.html`。
2. 选择“价格列表查询.xlsx”。
3. 选择“物料主数据.xlsx”。
4. 两个文件选齐后会自动合并。
5. 检查页面显示的记录数和异常数，点击“导出 Excel”。

也可以双击：

- macOS：`打开工具.command`
- Windows：`打开工具.bat`

## 合并规则

- 以“价格列表查询”为基础，保留其中全部物料记录和原有顺序。
- 以“物料编码”为主要匹配键。
- 同时核对“物料名称”和“规格”是否一致。
- 输出字段固定为：物料编码、物料名称、规格、单价、物料业务状态。
- 找不到编码时仍会保留价格记录，并把状态标记为“未匹配”。
- 名称或规格不一致时仍按物料编码匹配，但会在结果页提示异常数量。

## 文件要求

- 当前支持 `.xlsx`。
- 价格表必须包含：`物料编码`、`物料名称`、`规格`、`单价`。
- 主数据必须包含：`物料编码`、`物料名称`、`规格`、`物料业务状态`。
- 列的位置可以变化，工具会根据表头名称识别。

## 大文件说明

工具采用分段读取方式，只从物料主数据中提取匹配需要的字段。处理 80MB 左右的源文件通常需要几十秒，期间请不要关闭页面。合并与导出 Excel 不需要联网。

推荐使用最新版 Chrome、Microsoft Edge 或 Safari。如果浏览器提示不支持分段解压，请改用最新版 Chrome 或 Edge。

## 文件夹说明

请保留以下内容在同一个文件夹内：

- `上市产品价格合并工具.html`
- `app.js`
- `styles.css`
- `vendor` 文件夹

`vendor` 中包含离线导出 Excel 所需的 SheetJS Community Edition 0.20.3 及其 Apache 2.0 许可证。
